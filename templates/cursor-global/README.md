# Cursor global templates (Omni-Protocol)

Used by `npm run omni:bootstrap`.

| File | Role |
|------|------|
| **`mcp.omni.json`** | MCP keys merged into `~/.cursor/mcp.json` only when those keys are missing |

Optional Linear or Docker MCP: add them manually or merge from your other machine. Do not commit real API keys into git.
