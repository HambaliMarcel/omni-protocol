# Context routing (priority)

See also `context-map/README.md` in repo root.

1. Structural — `architecture/SYSTEM_OVERVIEW.md`, `graphs/*.json`, GitNexus outputs.
2. Decisions — `adr/`, newest first.
3. Intent — `specs/`.
4. Contracts — `openapi/openapi.yaml`.
5. External truth — `external-docs/` (mirrored only).
6. Runtime lessons — `docs/telemetry/` + local PostHog queries.

Compression tip: paste **paths** and short excerpts, not whole generated JSON blobs.
