# BookStack local stack (OSS)

Community image with MariaDB. For pure local dev; add TLS + backups before anything sensitive.

```bash
cd infra/bookstack
docker compose up -d
```

Default URL (see compose): http://localhost:6875

## IA suggestion

- **Runbooks** — ops procedures
- **Omni-Protocol** — mirror of key ADR/spec links
- **Onboarding** — environment setup
